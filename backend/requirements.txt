# ============================================================
# HIRE — Python Dependencies
# ============================================================

# --- Core Framework ---
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0.2
slowapi>=0.1.9
httpx>=0.27.0

# --- Database ---
psycopg[binary,pool]>=3.1.0
sqlalchemy>=2.0.0

# --- AI / LLM ---
openai>=1.30.0
agno[postgres]>=2.5.0

# --- Document Processing ---
pymupdf>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
Pillow>=10.0.0

# --- OCR (free, local) ---
paddleocr>=2.7.0
paddlepaddle>=2.6.0
# Burmese (Myanmar) support: Zawgyi-encoded legacy text + Unicode normalization.
# myanmar-tools = Google's Zawgyi detector (statistical model).
# PyICU = ICU bindings; provides the `Zawgyi-my` transliterator.
# Both required together. PyICU needs system libicu-dev (added in Dockerfile).
myanmartools>=0.1
PyICU>=2.13

# --- Auth ---
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
pyjwt>=2.8.0

# --- Data ---
tqdm>=4.66.0

# --- AWS (only used when STORAGE_BACKEND=s3 or LOAD_SSM=1) ---
# Lazy-imported inside backend/core/storage.py + aws_secrets.py, so installs
# without AWS credentials still work locally.
boto3>=1.34.0

# --- Testing ---
pytest>=8.0.0

# --- Agno agent framework (chat v2, flag-gated) ---
agno>=1.0
redis>=5.0
